#встроенная_функция #Python #Unicode #кодировка #метод #chr

Метод возвращает символ, чья позиция в таблице Unicode соответствует переданному числу
```python
print(chr(97))
print(chr(66))
```
```
a
B
```