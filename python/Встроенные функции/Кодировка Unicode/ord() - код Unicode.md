#встроенная_функция #Python #Unicode #кодировка #ord #метод

Метод возвращает число, представляющее позицию переданного символа в таблице Unicode
```python
print(ord('a'))
print(ord('B'))
```
```
97
66
```

Обратный метод метода `chr()`