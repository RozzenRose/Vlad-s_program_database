`pip` - это самый популярный менеджер пакетов. Он позволяет устанавливать, удалять и трекать различные пакеты и библиотеки для интерпретатора `python`.

## 🧩 1. Установка и удаление пакетов

| Команда                           | Назначение                                    |
| --------------------------------- | --------------------------------------------- |
| `pip install package_name`        | Установить пакет из PyPI                      |
| `pip install package==1.2.3`      | Установить конкретную версию                  |
| `pip install "package>=1.0,<2.0"` | Установить пакет в диапазоне версий           |
| `pip install -U package`          | Обновить уже установленный пакет              |
| `pip uninstall package`           | Удалить пакет                                 |
| `pip install -r requirements.txt` | Установить зависимости из файла               |
| `pip freeze > requirements.txt`   | Сохранить список установленных пакетов в файл |
## 🔍 2. Проверка и информация о пакетах

|Команда|Назначение|
|---|---|
|`pip list`|Показать список установленных пакетов|
|`pip show package_name`|Подробная информация о пакете|
|`pip check`|Проверить наличие конфликтов версий|
|`pip freeze`|Показать список пакетов в формате для `requirements.txt`|
|`pip index versions package_name`|Показать все доступные версии пакета|
## ⚙️ 3. Работа с самим `pip`

|Команда|Назначение|
|---|---|
|`pip --version`|Показать версию `pip` и путь|
|`pip install --upgrade pip`|Обновить `pip`|
|`pip config list`|Показать текущие настройки `pip`|
|`pip cache dir`|Показать путь к кэшу пакетов|
|`pip cache purge`|Очистить кэш|
## 📦 4. Установка из нестандартных источников

|Команда|Назначение|
|---|---|
|`pip install ./local_folder`|Установить пакет из локальной папки|
|`pip install https://example.com/package.tar.gz`|Установить из URL|
|`pip install git+https://github.com/user/repo.git`|Установить напрямую из GitHub|