#ddd #infrastructure_layer

Этот слой хранит различные объекты, которые нужны для работы различных фреймворков или библиотек, как например алхимический движек или сешнмейкер. Так же на этом слое хрянятся алхимические модели данных и репозитории. Так же я там храню `di`-шные зависимости, хз насколько это оправданно с точки зрения чистоты архитектуры, возможно это грязное решение.

Примерная структура слоя:
![[Pasted image 20260312151623.png]]

Расписывать тут что такое движек и сешнмейкер я не буду, Про алхимические модели расписывать тоже не буду. Про это все в этой базе уже достаточно написано в статьях по `SQLAlchemy`. 

Зато про `di`-шные зависимости поясню.

Вот пример:
```python
async def get_db():
    async with session_factory() as session:
        yield session


def get_account_repo(
        session=Depends(get_db)
):
    return SqlAlchemyAccountRepo(session)


def get_create_account_usecase(
        repo=Depends(get_account_repo)
):
    return CreateAccountUseCase(repo)
```
У нас есть функция, которая создает сессию `bd`, эта функция становится зависимостью для функции, которая создает экземепляр репозитория. А экземпляр репозитория становится зависимостью для `usecase`