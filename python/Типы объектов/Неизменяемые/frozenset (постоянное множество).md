#неизменяемый_объект #объект #тип_данных #python #итерируемый_объект 


Работает точно так же как объекты типа `set`, но при этом его нельзя изменять.
```python
a = set('qwerty')
b = frozenset('qwerty')
print(a == b)  # Выведет True
```